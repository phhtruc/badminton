spring:
  application:
    name: badminton-booking-test

  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-falling-paper-adtgusy1-pooler.c-2.us-east-1.aws.neon.tech:5432/badminton?sslmode=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_hrDC6uqUsLl2}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: test@example.com
    password: test-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:TestSecretKeyForJWTTokenGenerationAtLeast256BitsLong12345678901234567890}
  expiration: 86400000

# Admin credentials for test
admin:
  email: ${ADMIN_EMAIL:admin@test.com}
  password: ${ADMIN_PASSWORD:Admin@123}
  name: ${ADMIN_NAME:Test Administrator}
